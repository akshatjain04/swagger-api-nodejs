# Test generated by RoostGPT for test go-test using AI Type Open AI and AI Model gpt-4

# RoostTestHash=ed0d766b50

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  plugins:
    expect: {}
  payload:
    path: "products_get_others.csv"
    fields:
      - "name"
      - "description"
      - "price"
    skipHeader: true

scenarios:
  - name: "Get All Products failure and edge cases"
    flow:
      - log: "Sending a request with an invalid URL"
        get:
          url: "/productss"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
      - think: 1

      - log: "Sending a request without authentication"
        get:
          url: "/products"
          expect:
            - statusCode: 401
      - think: 1

      - log: "Sending a request with invalid authentication credentials"
        get:
          url: "/products"
          headers:
            Authorization: "Bearer invalidtoken"
          expect:
            - statusCode: 401
      - think: 1

      - log: "Sending a request with a missing required header"
        get:
          url: "/products"
          headers:
            missingheader: "missing value"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
      - think: 1

      - log: "Sending a request with an unsupported HTTP method"
        post:
          url: "/products"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 405
      - think: 1

      - log: "Sending a request with a malformed header"
        get:
          url: "/products"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
            malformedheader: "malformed value"
          expect:
            - statusCode: 400
      - think: 1
