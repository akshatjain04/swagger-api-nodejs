# Test generated by RoostGPT for test go-test using AI Type Open AI and AI Model gpt-4

# RoostTestHash=47d9fe845f

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "products_productId_delete_others.csv"
    fields:
      - "productId"
    skipHeader: true

scenarios:
  - name: "Delete product failure cases"
    flow:
      - think: 1
      - log: "Attempt to delete with invalid product id"
      - delete:
          url: "/products/{{ productId }}invalid"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "description"
      - think: 1
      - log: "Attempt to delete with invalid url"
      - delete:
          url: "/invalidUrl/{{ productId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
      - think: 1
      - log: "Attempt to delete with invalid token"
      - delete:
          url: "/products/{{ productId }}"
          headers:
            Authorization: "InvalidToken"
          expect:
            - statusCode: 401
