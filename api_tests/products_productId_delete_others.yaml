# Test generated by RoostGPT for test go-test using AI Type Open AI and AI Model gpt-4

# RoostTestHash=47d9fe845f

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "products_productId_delete_others.csv"
    fields:
      - "productId"
    skipHeader: true
  plugins:
    expect: {}
scenarios:
  - name: "Delete product scenario"
    flow:
      - log: "Deleting a product with non-existing Product id"
        delete:
          url: "/products/{{ productId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
        expect:
          - statusCode: 404
          - hasProperty: "message"
          - equals: 
              message: "Not Found"
      - think: 5
      - log: "Deleting a product with invalid token"
        delete:
          url: "/products/{{ productId }}"
          headers:
            Authorization: "InvalidToken"
        expect:
          - statusCode: 401
          - hasProperty: "message"
          - equals: 
              message: "Unauthorized"
      - think: 5
      - log: "Deleting a product with invalid URL"
        delete:
          url: "/invalidURL/{{ productId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
        expect:
          - statusCode: 404
          - hasProperty: "message"
          - equals: 
              message: "Not Found"
