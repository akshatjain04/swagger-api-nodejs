# Test generated by RoostGPT for test go-test using AI Type Open AI and AI Model gpt-4

# RoostTestHash=47d9fe845f

config:
  target: "{{ $processEnvironment.API_HOST }}"
  processor: './my-processor.js'
  payload:
    path: "products_productId_delete_others.csv"
    fields:
      - "productId"
    skipHeader: true
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
scenarios:
  - name: "Delete product scenario"
    flow:
      - log: "Testing with invalid product id"
        delete:
          url: "/products/{{ productId }}"
          headers:
            Authorization: "InvalidAuth {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: 'error'
        # Expected outcome: API should return 404 Not Found error with error message in the response body
      - think: 5
      - log: "Testing with invalid URL"
        delete:
          url: "/invalidURL/{{ productId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: 'error'
        # Expected outcome: API should return 404 Not Found error with error message in the response body
      - think: 5
      - log: "Testing with missing product id in the URL"
        delete:
          url: "/products/"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: 'error'
        # Expected outcome: API should return 404 Not Found error with error message in the response body
      - think: 5
