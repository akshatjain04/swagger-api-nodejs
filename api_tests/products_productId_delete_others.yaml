# Test generated by RoostGPT for test go-test using AI Type Open AI and AI Model gpt-4

# RoostTestHash=47d9fe845f

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "products_productId_delete_others.csv"
    fields:
      - "productId"
    skipHeader: true
  http:
    pool: 10
scenarios:
  - name: "Delete product API failure and edge case tests"
    flow:
      - log: "Sending a DELETE request with a non-existing product ID"
        delete:
          url: "/products/{{ productId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "description"
            - equals: 
                - "description"
                - "Not Found"
        think: 5

      - log: "Sending a DELETE request with an invalid product ID"
        delete:
          url: "/products/invalidId"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "description"
            - equals: 
                - "description"
                - "Not Found"
        think: 5

      - log: "Sending a DELETE request with an invalid URL"
        delete:
          url: "/invalidURL/{{ productId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
        think: 5

      - log: "Sending a DELETE request without authorization"
        delete:
          url: "/products/{{ productId }}"
          expect:
            - statusCode: 401
        think: 5

      - log: "Sending a DELETE request with invalid authorization"
        delete:
          url: "/products/{{ productId }}"
          headers:
            Authorization: "InvalidToken"
          expect:
            - statusCode: 403
        think: 5
