# Test generated by RoostGPT for test go-test using AI Type Open AI and AI Model gpt-4

# RoostTestHash=47d9fe845f

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "products_productId_delete_others.csv"
    fields: ["productId"]
    skipHeader: true

scenarios:
  - name: "Delete product test scenario"
    flow:
      - log: "Attempting to delete a product with invalid product id"
        delete:
          url: "/products/{{ productId }}"
          headers:
            Authorization: "Bearer invalid_token"
          expect:
            - statusCode: 401 # Expect Unauthorized error due to invalid token
            - hasProperty: "message"
            - hasProperty: "statusCode"
        think: 1

      - log: "Attempting to delete a product with non-existing product id"
        delete:
          url: "/products/9999999999"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404 # Expect Not Found error for non-existing product id
            - hasProperty: "message"
            - hasProperty: "statusCode"
        think: 1

      - log: "Attempting to delete a product with invalid URL"
        delete:
          url: "/invalid_url/{{ productId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404 # Expect Not Found error for invalid URL
            - hasProperty: "message"
            - hasProperty: "statusCode"
        think: 1

      - log: "Attempting to delete a product without providing product id"
        delete:
          url: "/products/"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400 # Expect Bad Request error for missing product id
            - hasProperty: "message"
            - hasProperty: "statusCode"
        think: 1
