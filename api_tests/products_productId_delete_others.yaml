# Test generated by RoostGPT for test go-test using AI Type Open AI and AI Model gpt-4

# RoostTestHash=47d9fe845f

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "products_productId_delete_others.csv"
    fields:
      - "productId"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Delete product scenario"
    flow:
      - log: "Sending a delete request with a non-integer productId"
        delete:
          url: "/products/abc"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "message"

      - think: 1

      - log: "Sending a delete request with a product that does not exist"
        delete:
          url: "/products/{{ productId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "message"

      - think: 1

      - log: "Sending a delete request without authorization"
        delete:
          url: "/products/{{ productId }}"
          headers:
            Authorization: "Invalid"
          expect:
            - statusCode: 401
            - hasProperty: "message"

      - think: 1

      - log: "Sending a delete request with an invalid URL"
        delete:
          url: "/invalid/products/{{ productId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "message"

      - think: 1

      - log: "Sending a delete request with an unsupported HTTP method"
        get:
          url: "/products/{{ productId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 405
            - hasProperty: "message"
