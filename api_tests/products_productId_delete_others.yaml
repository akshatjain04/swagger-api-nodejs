# Test generated by RoostGPT for test go-test using AI Type Open AI and AI Model gpt-4

# RoostTestHash=47d9fe845f

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "products_productId_delete_others.csv"
    fields:
      - "productId"
    skipHeader: true
  plugins:
    expect: {}
scenarios:
  - name: "Delete product tests"
    flow:
      # Case: Invalid product ID
      - log: "Sending a request with an invalid product ID"
        delete:
          url: "/products/{{ productId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "error"
            - hasHeader: "Content-Type"
        think: 1
      # Case: Invalid URL
      - log: "Sending a request to an invalid URL"
        delete:
          url: "/invalid/{{ productId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "error"
            - hasHeader: "Content-Type"
        think: 1
      # Case: Invalid authorization token
      - log: "Sending a request with an invalid authorization token"
        delete:
          url: "/products/{{ productId }}"
          headers:
            Authorization: "InvalidToken"
          expect:
            - statusCode: 401
            - hasProperty: "error"
            - hasHeader: "Content-Type"
        think: 1
