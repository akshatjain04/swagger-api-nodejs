# Test generated by RoostGPT for test go-test using AI Type Open AI and AI Model gpt-4

# RoostTestHash=47d9fe845f

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  plugins:
    expect: {}
  payload:
    path: "products_productId_delete_others.csv"
    fields: ["productId"]
    skipHeader: true

scenarios:
  - flow:
      - log: "Delete request with invalid product ID"
      - delete:
          url: "/products/{{ productId }}"
          headers:
            Authorization: "invalid_token"
          capture:
            - json: "$"
              as: "response"
          expect:
            - statusCode: 404
            - hasProperty: "description"
      - think: 5
      - log: "Delete request with non-existent product ID"
      - delete:
          url: "/products/999999999999"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          capture:
            - json: "$"
              as: "response"
          expect:
            - statusCode: 404
            - hasProperty: "description"
      - think: 5
      - log: "Delete request with invalid URL"
      - delete:
          url: "/invalid_url/{{ productId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          capture:
            - json: "$"
              as: "response"
          expect:
            - statusCode: 404
            - hasProperty: "description"
