# Test generated by RoostGPT for test go-test using AI Type Open AI and AI Model gpt-4

# RoostTestHash=42ac6fa2e0

config:
  target: '{{ $processEnvironment.API_HOST }}'
  plugins:
    expect: {}
  payload:
    path: Profile_accountCode_post_success.csv
    fields:
      - accountCode
      - firstName
      - lastName
      - profilePin
    skipHeader: true
  phases:
    - duration: 60
      arrivalRate: 20
scenarios:
  - name: Create new profile for specified account
    flow:
      - log: >-
          Making a POST request to /Profile/{accountCode} endpoint with valid
          payload
        post:
          url: /Profile/{{ accountCode }}
          headers:
            Authorization: '{{ $processEnvironment.AUTH_TOKEN }}'
            Content-Type: application/json
          json:
            firstName: '{{ firstName }}'
            lastName: '{{ lastName }}'
            profilePin: '{{ profilePin }}'
          expect:
            - statusCode: 200
            - hasProperty: firstName
            - hasProperty: lastName
            - hasProperty: profilePin
          capture:
            - json: $.firstName
              as: actualFirstName
            - json: $.lastName
              as: actualLastName
            - json: $.profilePin
              as: actualProfilePin
      - think: 1
      - log: >-
          Checking if the response body has the correct firstName, lastName, and
          profilePin
        think: 1
