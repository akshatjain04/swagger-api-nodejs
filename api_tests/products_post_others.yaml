# Test generated by RoostGPT for test go-test using AI Type Open AI and AI Model gpt-4

# RoostTestHash=2c080099fd

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "products_post_others.csv"
    fields: ["id", "name", "description", "price"]
    skipHeader: true

scenarios:
  - flow:
    - log: "API request with invalid payload"
    - post:
        url: "/products"
        json:
          id: "xyz"
          name: "abc"
          description: "desc"
          price: "123"
        headers:
          Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
        expect:
          - statusCode: 400
          - hasProperty: "error"
    - think: 1

    - log: "API request with edge case"
    - post:
        url: "/products"
        json:
          id: "{{ id }}"
          name: "{{ name }}"
          description: "{{ description }}"
          price: "{{ price }}"
        headers:
          Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
        expect:
          - statusCode: 500
          - hasProperty: "error"
    - think: 1

    - log: "API request with unauthorized access"
    - post:
        url: "/products"
        json:
          id: "{{ id }}"
          name: "{{ name }}"
          description: "{{ description }}"
          price: "{{ price }}"
        headers:
          Authorization: "Bearer invalid_token"
        expect:
          - statusCode: 401
          - hasProperty: "error"
    - think: 1

    - log: "API request with unsupported HTTP method"
    - get:
        url: "/products"
        headers:
          Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
        expect:
          - statusCode: 405
          - hasProperty: "error"
    - think: 1

    - log: "API request with invalid URL"
    - post:
        url: "/invalid_url"
        json:
          id: "{{ id }}"
          name: "{{ name }}"
          description: "{{ description }}"
          price: "{{ price }}"
        headers:
          Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
        expect:
          - statusCode: 404
          - hasProperty: "error"
    - think: 1
