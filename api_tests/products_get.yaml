# Test generated by RoostGPT for test go-test using AI Type Open AI and AI Model gpt-4

# RoostTestHash=e7f295bc70

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
scenarios:
  - name: "Successful API request"
    flow:
      - get:
          url: "/products"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "name"
            - hasProperty: "description"
            - hasProperty: "price"

  - name: "API request with validation failure"
    flow:
      - get:
          url: "/invalid_url"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404

  - name: "API request with edge case"
    flow:
      - get:
          url: "/products"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "name"
            - hasProperty: "description"
            - hasProperty: "price"

  - name: "API request without authentication"
    flow:
      - get:
          url: "/products"
          expect:
            - statusCode: 401

  - name: "API request with invalid authentication credentials"
    flow:
      - get:
          url: "/products"
          headers:
            Authorization: "Invalid token"
          expect:
            - statusCode: 401

  - name: "API request by a user with insufficient permissions"
    flow:
      - get:
          url: "/products"
          headers:
            Authorization: "{{ $processEnvironment.INSUFFICIENT_PERMISSIONS_TOKEN }}"
          expect:
            - statusCode: 403
