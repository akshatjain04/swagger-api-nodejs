# Test generated by RoostGPT for test go-test using AI Type Open AI and AI Model gpt-4

# RoostTestHash=6762b8ac71

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "products_productId_put_others.csv"
    fields: ["id", "name", "description", "price"]
    skipHeader: true

scenarios:
  - name: "scenario"
    flow:
      - think: 5

      - log: "Sending request with missing required fields"
      - put:
          url: "/products/{{ id }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/json"
          json:
            name: "{{ name }}"
            price: "{{ price }}"
          expect:
            - statusCode: 400
            - hasProperty: "message"
            - equals:
                message: "Field 'description' is missing"
      - think: 5

      - log: "Sending request with large data in fields"
      - put:
          url: "/products/{{ id }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/json"
          json:
            id: "{{ id }}"
            name: "{{ name }}"
            description: "{{ description }}"
            price: "{{ price }}"
          expect:
            - statusCode: 413
      - think: 5

      - log: "Sending request with wrong data type in fields"
      - put:
          url: "/products/{{ id }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/json"
          json:
            id: "{{ id }}"
            name: "{{ name }}"
            description: "{{ description }}"
            price: "wrong type"
          expect:
            - statusCode: 400
            - hasProperty: "message"
            - equals:
                message: "Field 'price' should be a number"
      - think: 5

      - log: "Sending request without authentication"
      - put:
          url: "/products/{{ id }}"
          headers:
            Content-Type: "application/json"
          json:
            id: "{{ id }}"
            name: "{{ name }}"
            description: "{{ description }}"
            price: "{{ price }}"
          expect:
            - statusCode: 401
      - think: 5

      - log: "Sending request with invalid authentication credentials"
      - put:
          url: "/products/{{ id }}"
          headers:
            Authorization: "Invalid token"
            Content-Type: "application/json"
          json:
            id: "{{ id }}"
            name: "{{ name }}"
            description: "{{ description }}"
            price: "{{ price }}"
          expect:
            - statusCode: 401
      - think: 5

      - log: "Sending request to a non-existing product"
      - put:
          url: "/products/9999999999999"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/json"
          json:
            id: "{{ id }}"
            name: "{{ name }}"
            description: "{{ description }}"
            price: "{{ price }}"
          expect:
            - statusCode: 404
