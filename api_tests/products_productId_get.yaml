# Test generated by RoostGPT for test go-test using AI Type Open AI and AI Model gpt-4

# RoostTestHash=fe22099285

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "products_productId_get.csv"
    fields:
      - "productId"
    skipHeader: true

scenarios:
  - name: "Successful API request"
    flow:
      - get:
          url: "/products/{{ productId }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "name"
            - hasProperty: "description"
            - hasProperty: "price"

  - name: "API request with validation failure"
    flow:
      - get:
          url: "/products/invalid"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 404

  - name: "API request with edge case"
    flow:
      - get:
          url: "/products/99999999999999999999"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 404

  - name: "API request without authentication"
    flow:
      - get:
          url: "/products/{{ productId }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 401

  - name: "API request with invalid authentication credentials"
    flow:
      - get:
          url: "/products/{{ productId }}"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer invalid_token"
          expect:
            - statusCode: 401

  - name: "API request with insufficient permissions"
    flow:
      - get:
          url: "/products/{{ productId }}"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer insufficient_permissions_token"
          expect:
            - statusCode: 403

  - name: "API request with unsupported HTTP method"
    flow:
      - post:
          url: "/products/{{ productId }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 405

  - name: "API request with malformed headers"
    flow:
      - get:
          url: "/products/{{ productId }}"
          headers:
            Content-Type: "malformed"
          expect:
            - statusCode: 400
